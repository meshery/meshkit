syntax = "proto3";

package controller;

option go_package = "github.com/layer5io/meshkit/protobuf/controller;controller";

enum ControllerStatus {
	RUNNING = 0;
	IDLE = 1;
}

message ControllerHealth {
    ControllerStatus status = 1;
}
